# double_pinyin_flypy.custom.yaml 示例
# 适用于小鹤双拼方案

patch:
  # 1. 注册 Lua 过滤器：在过滤器列表末尾添加股票查询插件
  "engine/filters/@next": lua_filter@stock_filter

  # 2. 词库配置：启用股票搜索功能
  # 推荐使用「追加模式」：
  # 1) 将 luna_pinyin.extended.dict.yaml 放入用户目录
  # 2) 确保配置如下：
  "translator/dictionary": double_pinyin_flypy.extended
