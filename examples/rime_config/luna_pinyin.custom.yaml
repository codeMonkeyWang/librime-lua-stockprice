# luna_pinyin.custom.yaml 示例
# 适用于朙月拼音方案

patch:
  # 1. 注册 Lua 过滤器：在过滤器列表末尾添加股票查询插件
  "engine/filters/@next": lua_filter@stock_filter

  # 2. 词库配置：启用股票搜索功能（通过拼音、代码搜索）
  # 推荐使用「追加模式」：
  # 1) 将 luna_pinyin.extended.dict.yaml 放入用户目录
  # 2) 取消下方行的注释：
  "translator/dictionary": luna_pinyin.extended
