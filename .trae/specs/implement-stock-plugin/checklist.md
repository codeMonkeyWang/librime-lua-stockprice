- [x] `stock_plugin.lua` 能够正确加载 `all_stocks.lua` 并建立索引。
- [x] 当第一个候选词匹配股票名称时，能够成功获取实时行情。
- [x] 行情候选词正确插入在第二个位置。
- [x] 行情格式符合要求（价格 + 涨跌幅）。
- [x] 网络超时或 API 错误时，插件不会导致输入法崩溃或长时间卡顿。
- [x] `rime.lua` 已正确配置并导出 `stock_filter`。
- [x] 在不匹配股票名称时，原始候选词列表顺序保持不变。
